"use client";

import QRTemplatesLayout from '../layout';

export default function CreateTemplateLayout({ children }) {
  // No additional layout - directly pass children through
  return children;
} 